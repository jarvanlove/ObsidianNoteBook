# Claude 使用偏好

## 核心原则

**执行习惯**
- 本地不需要进行环境搭建
- 在修改UI这个工作中需要使用gstack进行跟踪管理，实时告诉我进度
- 修改的是页面，所有功能不能有一丝丝改变

**代码规范**
- 提交代码时候先提交到dev分支，不要合并到master

**沟通方式**
- 告诉我刚刚修改了哪些文件

---

## 完整偏好列表（共259条）

- 创建 dev 分支基于 master，提交代码到 dev 分支，不要合并到 master
- 使用 gstack 进行进度跟踪管理，实时告知进度
- 不需要全部重改，别人的交互设计很棒的需要保留
- 只要更换主题整体风格，避免被识别为复刻
- 文档要符合 obsidian 双向链接
- 路径结构：C:\Work\note\ObsidianNoteBook\记忆库\语义记忆\claude-code-sourcemap-main
- claude-code-2.1.88.tgz不要提交到GitHub
- 本地不需要进行环境搭建
- 提交代码时先提交到dev分支，不要合并到master
- 只需要修改整体风格和细节，不要全部重改
- 别人好的交互设计要保留
- 整体更换主题避免被认为是复刻
- 先确定主题基调，然后依次修改各页面
- 不需要改动的地方就没必要改
- 使用gstack跟踪管理进度并实时报告
- 改好一个版本后用户在服务器部署前端服务再精细化修改
- 修改文件后需要告诉我详细文件路径
- 不要出现 ScienceClaw 相关引用
- 前端端口是 5174 不是 5173
- 需要返回标准 JSON 错误格式
- 修改文件后告诉我详细文件路径
- 不要放在左边侧边栏当作入口
- 修复所有问题后再进行/qa → /ship流程
- 整个流程需要告诉我
- 不要出现ScienceClaw相关描述
- 不要出现100%复刻字样
- 一次性解决问题，不要重复修改
- 严格按照集成步骤文档执行
- 页面太丑需要优化
- 优化页面设计和交互
- 先读取文件再写入
- 不要像煞笔一样
- 一次性解决问题，不要反复修改导致更多错误
- 严格按照README中的集成步骤执行
- 一次性解决问题，不要反复出错
- 认真按照README集成步骤操作
- 不要越改报错越多
- 遵循项目现有规范
- 页面不能丑
- TypeScript需要转换为纯JavaScript以兼容科研平台
- 直接动手不要废话
- 处理问题要快不要拖延
- 科研平台API是纯JavaScript，不支持TypeScript语法，需要转换为纯JavaScript版本
- 处理速度太慢，40分钟还没完成，用户催促'兄弟！！'
- 不要干涉，应该直接解决问题
- 页面'针的太丑了'，需要优化页面设计和交互
- 科研平台API是纯JavaScript，需要转换为纯JavaScript版本而非TypeScript
- 修改文件前需要先读取文件内容
- 页面针的太丑了，要优化页面和交互
- 模型名称配置需要写入全局配置文件 .env
- .env.example 也需要一并更新
- 模型名称需要写入全局配置文件 .env 和 .env.example
- 服务器的 pg 数据库非常重要，不允许出问题
- 选择下拉框数据后不能改变原有页面布局
- 刷新页面后要保持原始布局，不能混乱
- 修改后需列出所有改动的文件完整路径
- 不需要查看git提交记录，与git历史无关
- 选择下拉框数据后不能改变原有页面布局，必须保持原样
- 视频监控模块选择视频后要能直接播放
- 修改后要列出所有修改的文件完��路径
- 不要通过查git提交记录来排查问题
- 修改代码后不能改变原有页面布局
- 选择下拉框数据后视频应直接播放（而非仅全屏时播放）
- 页面刷新后应能恢复原始状态
- 先问再改 - 用户要求在修改文件前先确认
- 跳过权限确认 - 用户希望使用--dangerously-skip-permissions启动claude
- 手动输入完整参数 - 用户最终选择手动输入claude --dangerously-skip-permissions
- 统一配置管理 - 用户要求配置集中在一个文件中修改
- 使用python -m app.main启动项目
- 每做一件事要告诉我正在做什么
- 先总结已完成的需求再继续
- 配置集中在一个文件（.env）修改
- stitch MCP配置文件放在项目根目录 .mcp.json
- Docker部署使用PowerShell一键部署方式
- 提供完整安全的操作步骤
- 启动命令用 python -m app.main
- SQL文件确保可执行无语法错误
- 需要明确告知完成事项
- 不要盲目猜测操作
- 配置集中在一个文件中管理
- 修改.env后同步更新.env.example
- 代码暂不写，先调研
- 部署命令需要完整版
- 先问再改
- 直接完成操作，不要让用户手动执行
- 不要随便连其他服务（如Figma），要按用户要求连接
- stitch必须连接上，在claude code中可以操作stitch
- 用户主动询问时才解释，避免主动说明在做什么
- 用户要求先检查再操作，不要直接执行
- 配置stitch MCP并告诉使用方法
- 检查PROGRESS.md根据最新gstack完成项目开发
- MCP命令不显示的问题
- 退出Claude后后台服务是否关闭
- 用户希望Claude帮助完成操作而不是只给指导，自己操作容易出错
- 用户会询问Claude正在做什么事情
- 我希望你告诉我启动命令
- 接下来要做什么？我替代需求你完成了哪些？
- 你帮我把函数添加好 我不想自己操作 会出错的
- claude 全局 setting.json 我已经配置了 skipDangerousModePermissionPrompt: true 为什么还要每次都确认
- 我不希望输入c或cl希望输入claude然后就相当于执行claude --dangerously-skip-permissions
- 将快捷启动c或cl修改为claude -d相当于claude --dangerously-skip-permissions
- PROGRESS.md这个是前天下午让你根据gstack来实现当前项目而做的一个文档 你可以检查一下 然后根据你更新的最新gstack来完成项目开发对接
- 我问你再做什麼事情时你要回答
- 继续代码库映射
- 代码提交到公司gitlab (http://192.168.1.241:9980/root/research-agent)
- 执行/mcp github连接失败要检查并修复
- 帮我配置stitch MCP (使用官方给出的集成命令)
- 分析一下项目目前的状况再行动手
- 数据库脚本需要包含每个字段的注释，且不能执行报错
- 需要满足科研平台前端的目录结构
- 暂时使用项目级别配置，不要全局级别
- 修改不能太丑，需要有审美
- 恢复之前的重新设计方案
- 不要瞎编，要基于实际代码分析
- 修改要有审美，不能太丑
- 恢复重新设计
- 需要详细的开发计划
- 明确任务节点让用户清楚进度
- 不要编造需求
- 告诉我你要怎么做再动手
- 先分析项目现状再开发
- 需要明确的任务阶段控制
- 拒绝用户不满意的修改方案
- 需要详细的技术文档说明实现逻辑
- 与科研平台业务一致性需明确
- 我要了解你的实现逻辑，是否和科研平台的业务一致
- 难道不分析不出来？
- 你要认真思考并回答
- 脚本需要包含每个字段的注释，且不能执行报错
- 不是，哥们 你这修改的太丑了 你没有审美吗！先恢复之前的 重新设计方案修改
- 记得脚本需要包含每个字段的注释
- 暂时使用项目级别，你创建的通用全局级别的请去掉相关配置
- 我要知道到了那一步节点，你要如何控制，让我能准确知道
- 需要明确具体的操作命令
- 简化版解释概念
- 使用英文命令/secondbrain而非中文
- 先确认方案再执行
- 英文目录名不直接用作标签，需询问用户中文项目名
- 项目专属语义记忆用中文目录
- 支持SECONDBRAIN_PATH环境变量覆盖默认路径
- 只提交记忆库目录而非整个工作空间
- 禁止自动提交，触发词：commit/提交//commit
- gstack作为以后工作的第一选择（所有gstack）
- 中文目录名作为临时标签
- 永远不使用#未分类或#通用等垃圾堆标签
- 当用户说某个原因不是问题时，立即停止该方向并转向
- 优先编写代码而非反复提问；多次请求后，直接实现并在注释中注明假设
- 项目路径识别：英文目录名不直接用作��签，需询问用户中文项目名
- 语义记忆分类：项目专属写入语义记忆/<项目名>/，通用知识写入语义记忆/通用/，第二大脑系统知识写入语义记忆/第二大脑/
- 文件内标签使用中文项目名，非英文目录名
- GitHub 提交触发词：commit / 提交 / /commit，被动等用户指令
- 禁止自动提交（FATAL-005）
- 只提交：记忆库/（含任务清单、搭建指南、源文）
- 当用户说"启动第二大脑"时，读取任务清单显示今日概览
- 当用户说"记录这个决策"时，写入情景记忆并自动加#当前项目标签
- 当用户说"总结成经验"时，写入语义记忆并自动加#当前项目标签
- 当用户说"这是通用知识"时，写入记忆但不加项目标签
- 英文目录名(如Link-AI-Sci-Z-Platform-Backend)→询问用户中文项目名，不直接用作标签
- 中文目录名→临时标签为#目录名，等用户确认正式名称
- 首次确认项目名时同步更新~/.claude/projects/<path>/memory/MEMORY.md
- gstack作为以后工作的第一选择(所有gstack)
- 关于第二大脑系统本身→语义记忆/第二大脑/
- 跨项目通用知识→语义记忆/通用/
- 某项目专属经验→语义记忆/<该项目名>/
- 禁止自动提交(FATAL-005)，触发词：commit/提交/commit被动执行
- 只提交记忆库/(含任务清单、搭建指南、源文)
- 禁止自动提交(FATAL-005)，需用户明确触发commit/提交
- 只提交记忆库相关文件，不提交整个工作空间
- 项目专属语义记忆 → 写入语义记忆/<对应中文目录>/
- 通用知识 → 写入语义记忆/通用/
- 第二大脑系统自身积累 → 写入语义记忆/第二大脑/
- 解析按钮悬浮时背景色改为#1e3a8a（学术蓝），文字白色
- 原文内容字体颜色与文档上传模式保持一致
- i18n占位符统一修改为'请粘贴链接'（中英日韩4个文件）
- 链接解析优先使用Crawl4AI，若失败则使用Jsoup fallback
- 若方案一二均失败则使用付费API
- 技术架构方案、PRD、Phase1开发计划保持一致
- 先问再改 - 澄清问题时要先问用户再继续工作
- 按钮悬浮要有交互特效
- 背景颜色变化交互
- 文字大小和颜色要与设计一致
- 修改后要同步到toc和toc-wjw分支
- 使用 brainstorm skill 进行头脑风暴
- 使用team进行分工协作
- 用户多次催促加快进度，表达不满情绪
- 先问清楚需求再开始写东西
- 不要开始写东西，针对需求先告诉思路想法和解决方案
- 保持toc、toc-wjw、当前分支代码一致
- skill 要按照 Claude Code 官方标准格式来做
- skill 应将资源整合到一起打包好
- 动态发现方案
- README.md 和 SKILL.md 需要融合
- 角色文档放到 skill 的 data 目录下便于维护
- Cursor 中写好的代码想保留，在 Claude Code 中重新实现
- 首页优化暂时不要编写任何代码，只需提供建设性执行计划和意见
- 先分析项目前后端启动后前端默认页面是哪个
- 需要详细的执行计划，保证最终修改优化后的页面能够抓住用户眼球、突出科研主题
- 使用 kimi-k2.5 模型（通过国内中转）
- 内网可以使用明文密码
- 代码要一样
- 希望增加鼠标悬浮交互特效
- 给点背景颜色变化
- 通过截图展示问题
- skill应该将资源整合到一起打包好，按照Claude Code官方标准去做
- skill目录结构需要按照Claude Code官方标准格式
- README.md和SKILL.md可以融合
- 角色文档维护在skill的data目录下
- task-role-assignment skill采用动态发现方案
- 从word导入和从网页提取是两种不同的教案填写方式，针对不同学校设计，页面交互和文案应不同
- word导入执行后不能影响网页提取，反之亦然
- 无论word上传还是网页提取都要准确无误的匹配并自动填充到对应的教案表单
- 插件导入浏览器后要保证所有用户都能完整显示，不能出现兼容性问题
- 最起码需要兼容chrome和edge
- 登录注册页面要在保证浏览器兼容、完全显示页面的情况下不出现滚动条，因为滚动条会导致页面太长用户体验差
- 密码input框右边加眼睛按钮，点击可以显示密码，再次点击隐藏密码
- 只要用户在密码框输入有内容就要显示眼睛按钮
- 重置按钮的“重置”2个文字需要加上
- 新建模板按钮点击后弹窗右上角×按钮的主题请保持风格统一
- 从截图创建用户上传图片后图片右上角×主题请和其他保持一致
- 识别字段排版有点小问题需要稍微调整
- 下拉框的主题请和项目主题保持风格统一
- 创建模板时模板名称必填，请做好校验和交互
- 手动创建需查看示例和类型说明，需完善常见表单类型
- 字段对应表单类型是怎么做的需明确是大模型识别还是代码实现
- 页面流程需考虑模板创建和模板选择，提高用户体验
- plugin目录导入浏览器后，要保证不同用户都能完整显示页面，不能出现兼容性问题
- 登录注册页面要在保证浏览器兼容、完全显示页面的情况下不要出现滚动条
- 密码输入框只要有内容就要显示眼睛按钮，点击可以显示/隐藏密码
- 登录成功后应该显示用户的模板
- 退出登录后不应该提示未授权错误
- 修改路径时需要提供足够上下文以唯一标识位置
- 回复请以中文
- 点击一键登录按钮不需要提示是否统一服务协议，直接登录
- frontend-design、ui-ux-pro-max、find-skills、planning-with-files、documentation-writer这些skills要启用
- hooks要配置Windows兼容版本
- 使用自定义音效，不要回归系统自带音效
- 对速度很敏感
- 插件导入浏览器后要保证所���用户都能完整显示，不能出现兼容性问题
- 解析按钮悬浮背景色#1e3a8a文字白色
- 原文内容字体颜色与文档上传一致
- i18n占位符改为'请粘贴链接'
- 优先使用Crawl4AI解析链接
- 技术文档保持一致性
- 英文目录名→询问用户中文项目名，不直接用作标签
- gstack作为以后工作的第一选择
- 代码提交到公司gitlab (http://192.168.1.241:9980/root/research-agent 用户名：wjw 密码：wjw123456)
- 检查PROGRESS.md文档并根据最新gstack完成项目开发对接
- 帮我配置stitch MCP并告诉我怎么使用
- PROGRESS.md这个是前天下午让你根据gstack来实现当前项目而做的一个文档 你可以检查一下 ���后根据你更新的最新gstack来完成项目开发对接
- 跳过权限确认
- 手动输入完整参数
- 统一配置管理
- 选择下拉框数据后视频应直接播放
- 修改后要列出所有修改的文件完整路径
- 页面针的太丑了，要优化���面和交互
- 在修改UI这个工作中需要使用gstack进行跟踪管理，实时告诉我进度
- 修改的是页面，所有功能不能有一丝丝改变
- 提交代码时候先提交到dev分支，不要合并到master
- 告诉我刚刚修改了哪些文件
