# ZOE_Teaching_Assistant

**ZOE 教学助手项目**

📁 路径：`C:/Work/note/CursorWorkSpace/SALL/ZOE_Teaching_Assistant`

🔧 功能：
- 代码库映射
- GitLab 部署

---

### 2026-03-06

**需求：** 1.关于模板配置，目前数据库中已经初始化好一个模板的脚本，但是今天又接到一个学校需求，他们的模板和昨天初始化的模板不太一样，所以我想说的是，按照当前这样的需求，我们的功能要如何设计实现，因为产品做好上线之后，不是一个学校使用，而是很多学校，后面可能还会有更多学校，也就是需要配置更多的模板

- 实现：讨论了模板配置的灵活设计方案
- 技术：后端模板表设计

**需求：** 3.若是按照灵活填写的话，我希望页面再提取前面增加一步（模板初始化），用户根据自己学校官网的教案截图之后上传我们程序识别图片后，将图片中教案中的key全部解析然后写入模板表并关联用户id，这样就可以做到自动化、灵活

- 实现：提出了新增模板初始化步骤的方案
- 技术：AI图片识别

**需求：** 1.模型我打算使用 kimi-k2.5 这个模型有图文识别能力

- 实现：确认使用kimi-k2.5模型进行图片识别
- 技术：kimi-k2.5

**需求：** 1.不需要关注图片中字段的内容只需要关注字段名因为我们是模板2.还会存在一个问题，当我们识别后，有部分的表单格式不能自动填充，所以我需要你满足常见的表单格式，常见自动是被填充可以覆盖场景需求

- 实现：明确图片识别只提取字段名，需要支持常见表单格式的自动识别和填充
- 技术：AI图片识别 + 表单自动填充

**需求：** 1.plugin 目录中请使用sidepanel.html 2.修复sidepanel.html 中的登录，注册 可以按照 plugin_orgin 中的sidepanel.html 对应 登录 注册来修复 3.记住，目前只是先修复登录 注册

- 实现：使用sidepanel.html并修复登录注册功能
- 技术：Chrome插件前端


### 2026-03-07

**需求：** 如图，重置按钮 没有 重置文字 加上

- 实现：找到重置按钮代码位置（1706行），添加重置文字功能
- 技术：JavaScript/HTML

**需求：** 修改上传提示文字（去掉 Ctrl+V 粘贴说明）

- 实现：将所有 '支持 JPG、PNG 格式，也可拖拽或 Ctrl+V 粘贴' 替换为 '支持 JPG、PNG 格式，或拖拽图片到此处'
- 技术：JavaScript/HTML


### 2026-03-07

**需求：** 重置按钮没有重置文字加上

- 实现：在1706行找到button元素，用户添加了重置按钮的文字
- 技术：HTML/JS

**需求：** 上传提示文字修改为：支持 JPG、PNG 格式，或拖拽图片到此处

- 实现：将原提示文字'支持 JPG、PNG 格式，也可拖拽或 Ctrl+V 粘贴'替换为'支持 JPG、PNG 格式，或拖拽图片到此处'
- 技术：HTML/JS


### 2026-03-10

**需求：** 现在测试发现有一个问题，不通用户将plugin目录导入浏览器后，打开页面有的人可以显示完整，有点人不能显示完整，这个兼容性就有问题了，你要保证不能出现这种问题

- 实现：修改了sidepanel.html的CSS样式，添加overflow-y:auto等属性来修复不同浏览器的显示问题
- 技术：HTML/CSS

**需求：** 最起码需要兼容chrome edge

- 实现：创建了COMPATIBILITY.md文档记录兼容性问题，并进行了相应的CSS修复
- 技术：浏览器兼容性

**需求：** 登录注册页面希望在保证浏览器兼容、完全显示页面情况下不要出现滚动条？因为有了滚动条导致页面太长用户体验极差

- 实现：调整了.auth-screen的padding、max-height等CSS属性，优化了布局避免出现滚动条
- 技术：CSS

**需求：** 如图，密码这个input框右边加一个眼睛按钮，点击可以显示密码 再次点击隐藏密码

- 实现：在登录和注册表单中添加了密码显示/隐藏切换按钮，修改了HTML结构和CSS样式，点击眼睛图标可以切换密码type属性和图标显示状态
- 技术：HTML/CSS/JavaScript

**需求：** 眼睛按钮现在不显示了，不应管是用户在密码框只要输入有内容就要显示眼睛按钮吗

- 实现：修复了眼睛按钮的显示逻辑，在password-toggle类上添加样式确保输入内容时显示眼睛按钮
- 技术：CSS

**需求：** 帮我创建一个全局subagent,主要做superpowers:code-reviewer的事情，还需要做每次代码编写结束后要去检查代码是否符合业务要求，是否偷懒了，是否少写了，是否简单实现了，是否没有实现，然后将检查结果通知主进程

- 实现：创建了strict-code-reviewer.md agent配置文件，集成code-reviewer skill并在每次代码编写后进行严格检查
- 技术：Claude Code Agent配置


### 2026-03-10

**需求：** 现在测试发现有一个问题，不同用户将plugin目录导入浏览器后，打开页面有的人可以显示完整，有的人不能显示完整，这个兼容性就有问题了，你要保证不能出现这种问题

- 实现：修改CSS布局，使用position:absolute配合inset:0确保所有浏览器显示一致，创建COMPATIBILITY.md文档
- 技术：HTML/CSS

**需求：** 最起码需要兼容chrome edge

- 实现：确保CSS兼容性，使用标准的flex布局和position属性
- 技术：CSS

**需求：** 登录注册页面希望在保证浏览器兼容、完全显示页面情况下不要出现滚动条？因为有了滚动条导致页面太长用户体验极差

- 实现：调整.auth-screen的padding和布局，将overflow-y: auto改为overflow-y: auto以允许内部滚动而不产生外部滚动条
- 技术：CSS

**需求：** 如图，密码这个input框右边加一个眼睛按钮，点击可以显示密码 再次点击隐藏密码

- 实现：在密码input右侧添加眼睛按钮，修改input类型为text实现显示密码，点击切换type和按钮图标
- 技术：HTML/CSS/JavaScript

**需求：** 眼睛按钮现在不显示了啊！不应管是用户在密码框只要输入有内容就要显示眼睛按钮吗！

- 实现：修复眼睛按钮的显示逻辑，在密码框有内容时显示按钮
- 技术：JavaScript


### 2026-03-12

**需求：** 从word导入和模板没关系，现在选择模板上传文档AI填写显然是不合理的

- 实现：修改流程，word导入时跳过选择模板步骤
- 技术：前端HTML/JS

**需求：** 从word导入既然和模板没关系，为什么点击AI分析并填写还要提示请先从下拉框选择一个教案模板

- 实现：修改AI分析逻辑，word导入模式不检查模板选择
- 技术：前端JS逻辑

**需求：** 从word导入和从网页提取是两种不同的模式，页面交互、文案提醒也应不同

- 实现：区分word导入和网页提取两种模式的UI和文案
- 技术：前端HTML/CSS/JS

**需求：** 无论word上传还是网页提取都要准确无误的匹配并自动填充到对应的教案表单

- 实现：实现通用教案字段匹配系统，根据表单字段特征进行智能匹配和填充
- 技术：TypeScript后端服务+前端content script


### 2026-03-13

**需求：** 我要把代码提交到 GitLab，地址：http://192.168.1.241:9980/root/teaching_assistant，用户名：wjw，密码：wjw123456

- 实现：初始化 Git 仓库，配置远程仓库，解决权限问题后成功推送代码
- 技术：Git, GitLab

